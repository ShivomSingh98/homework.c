#include<stdio.h>
void main()
{
    int no, sum=0, digit;
    printf("enter a no:)");
    scanf("%d",&no);
    while(no!=0){
    digit=no%10;
    sum+=digit;
    no/=10;
    }
    printf("sum of digits:)%d",sum);
}
